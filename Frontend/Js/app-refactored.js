/**\n * Script principal da aplicação\n * Gerencia funcionalidades comuns\n */\n\ndocument.addEventListener('DOMContentLoaded', function () {\n    initializeApp();\n});\n\n/**\n * Inicializa a aplicação\n */\nfunction initializeApp() {\n    setCurrentYear();\n    checkAuthentication();\n    setupNavigationButtons();\n}\n\n/**\n * Define o ano atual no footer\n */\nfunction setCurrentYear() {\n    const yearElement = document.getElementById('currentYear');\n    if (yearElement) {\n        yearElement.textContent = new Date().getFullYear();\n    }\n}\n\n/**\n * Verifica autenticação e redireciona se necessário\n */\nfunction checkAuthentication() {\n    const isLoginPage = window.location.pathname.includes('login.html') || \n                        window.location.pathname.includes('index.html') ||\n                        window.location.pathname.includes('cadastro.html') ||\n                        window.location.pathname.includes('about.html');\n\n    if (!isLoginPage && !AuthService.isAuthenticated()) {\n        window.location.href = 'login.html';\n    }\n}\n\n/**\n * Configura eventos dos botões de navegação\n */\nfunction setupNavigationButtons() {\n    const homeBtn = document.getElementById('btn-home');\n    const principalBtn = document.getElementById('btn-principal');\n    const aboutBtn = document.getElementById('btn-sobre');\n\n    if (homeBtn) {\n        homeBtn.addEventListener('click', () => window.location.href = 'index.html');\n    }\n    if (principalBtn) {\n        principalBtn.addEventListener('click', () => window.location.href = 'principal.html');\n    }\n    if (aboutBtn) {\n        aboutBtn.addEventListener('click', () => window.location.href = 'about.html');\n    }\n}\n\n/**\n * Faz logout\n */\nfunction logout() {\n    if (confirm('Deseja realmente fazer logout?')) {\n        AuthService.logout();\n    }\n}\n\n/**\n * Formata data para exibição\n */\nfunction formatDate(date) {\n    return new Date(date).toLocaleDateString('pt-BR', {\n        year: 'numeric',\n        month: 'long',\n        day: 'numeric',\n        hour: '2-digit',\n        minute: '2-digit'\n    });\n}\n\n/**\n * Formata moeda\n */\nfunction formatCurrency(value) {\n    return new Intl.NumberFormat('pt-BR', {\n        style: 'currency',\n        currency: 'BRL'\n    }).format(value);\n}"